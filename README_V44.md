# ARC V13.44

本版取消傳真/領件收據順序所有防重限制：
- 同一領件日可使用相同收據順序。
- 不同領件日可使用相同收據順序。
- 加入預計、一鍵加入、單筆領件、已領件不再因收據順序重複被阻擋。
- 收據順序仍保留輸入、儲存、顯示、排序與列印。
- 仍需填寫收件編號、外字五碼、收據順序，僅取消「重複」限制。

部署前請先在 Supabase SQL Editor 執行：
`supabase/migrations/202607030016_arc_v13_v44_remove_receipt_order_limit.sql`
